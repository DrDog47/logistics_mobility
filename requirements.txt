# Web framework
Flask==3.0.3
gunicorn==23.0.0

# Database
SQLAlchemy==2.0.50
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
alembic==1.13.3
psycopg[binary]==3.2.13  # PostgreSQL driver (postgresql+psycopg:// URLs)

# Forms & validation
Flask-WTF==1.2.1
WTForms==3.1.2
email-validator==2.2.0
marshmallow==3.22.0

# Authentication
Flask-Login==0.6.3
bcrypt==5.0.0

# i18n
Flask-Babel==4.0.0
Babel==2.16.0
pycountry==24.6.1

# HTTP client (for NBP API)
httpx==0.27.2

# YAML rates files
PyYAML==6.0.2

# Environment & config
python-dotenv==1.0.1

# Document recognition (PRD §8.7) — Claude adapter (DOCUMENT_RECOGNIZER=claude).
# Imported lazily, so the default "fake" recognizer works without using it.
anthropic==0.107.1

# Google Calendar sync for driver vacations (optional). Imported lazily in
# app/vacations/google.py — manual-only mode works without these installed.
google-api-python-client==2.149.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1

# Dev/Test
pytest==8.3.3
pytest-flask==1.3.0
ruff==0.7.4
